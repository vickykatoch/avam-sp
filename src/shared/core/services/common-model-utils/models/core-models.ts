export interface ApplicationInfo {
      name              : string;
      type              : string;
      env               : string;
      region            : string;
      version           : string;
}